const express = require('express')
const router = express.Router()
const auth = require('../middleware.auth')


router.get('/', )

router.get('/:id', )

router.post('/', )

router.put('/:id', )

router.delete('/:id', )

router.post('/:id/like', )